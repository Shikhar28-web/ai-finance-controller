# Copy to .env (gitignored). The LLM writes display-only prose over already-computed
# evidence (SPEC 3). No metric depends on it; the pipeline runs fully without a key.
ANTHROPIC_API_KEY=
AFC_LLM_MODEL=claude-sonnet-5
